{
  "name": "jobak-scraper",
  "version": "0.1.0",
  "private": true,
  "description": "Job collection for Jobak. MENA boards, remote boards and applicant tracking systems behind one endpoint, plus company link enrichment.",
  "type": "module",
  "engines": {
    "node": ">=20.9.0"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "probe": "tsx scripts/probe.ts",
    "probe:apify": "tsx scripts/apify-probe.ts",
    "probe:enrich": "tsx scripts/enrich-probe.ts",
    "probe:sanitize": "tsx scripts/sanitize-probe.ts"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "@vercel/node": "^5.1.10",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3"
  }
}
